# FigureShot Lab — Frontend

Nuxt 4 + Pinia + @nuxtjs/i18n(en / zh-TW)+ Tailwind CSS 4。分層規則見 ../docs/frontend-architecture.md。

```bash
nvm use          # Node 22
npm install
npm run dev      # http://localhost:3000
npm run build
```
